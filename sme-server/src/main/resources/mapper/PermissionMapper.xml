<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.PermissionMapper">

    <!--    结果映射-->

    <resultMap id="PermissionResultMap" type="com.sme.entity.Permission">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="type" property="type" />
        <result column="parentId" property="parentId" />
        <result column="path" property="path" />
        <result column="component" property="component" />
        <result column="icon" property="icon" />
        <result column="sort" property="sort" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    <select id="findPermissionsByRoleId" parameterType="long" resultMap="PermissionResultMap">
        select * from sys_permission inner join role_permission on sys_permission.id = role_permission.permission_id
        where role_permission.role_id = #{roleId} and sys_permission.status = 1
    </select>


    <select id="findPermissionsByUserId" parameterType="long" resultMap="PermissionResultMap">
        select distinct * from sys_permission inner join role_permission on sys_permission.id = role_permission.permission_id
        inner join user_role on role_permission.role_id = user_role.role_id
        where user_role.user_id = #{userId} and sys_permission.status = 1
    </select>


    <select id="findByCode" resultType="com.sme.entity.Permission">
            select * from sys_permission where code = #{code} and status = 1
    </select>


</mapper>
