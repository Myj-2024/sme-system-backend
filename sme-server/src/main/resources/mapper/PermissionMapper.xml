<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.PermissionMapper">

    <!-- 结果映射 -->
    <resultMap id="PermissionResultMap" type="com.sme.entity.SysPermission">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="type" property="type" />
        <result column="path" property="path" />
        <result column="icon" property="icon" />
        <result column="sort" property="sort" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 分页查询 -->
    <select id="getPermissions" resultMap="PermissionResultMap">
        SELECT * FROM sys_permission
        WHERE del_flag = 0
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="code != null and code != ''">
            AND code LIKE CONCAT('%', #{code}, '%')
        </if>
        ORDER BY sort
    </select>

    <!-- 根据id查询 -->
    <select id="getPermissionById" resultMap="PermissionResultMap">
        SELECT * FROM sys_permission WHERE id = #{id}
    </select>

    <!-- 新增 -->
    <insert id="addPermission">
        INSERT INTO sys_permission (name, code, type, path, icon, sort, create_time, del_flag)
        VALUES (#{name}, #{code}, #{type}, #{path}, #{icon}, #{sort}, CURRENT_TIMESTAMP, 0)
    </insert>

    <!-- 更新 -->
    <update id="updatePermission">
        UPDATE sys_permission
        SET name = #{name}, code = #{code}, type = #{type}, path = #{path}, icon = #{icon},
            sort = #{sort}, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 删除（逻辑删除） -->
    <update id="deletePermission">
        UPDATE sys_permission
        SET del_flag = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 新增：路径唯一性校验SQL -->
    <select id="checkPathUnique" resultType="int">
        SELECT COUNT(1) FROM sys_permission WHERE path = #{path} AND del_flag = 0
    </select>

    <select id="selectAllPermissions" resultMap="PermissionResultMap">
        SELECT * FROM sys_permission
        WHERE del_flag = 0
        ORDER BY sort ASC
    </select>

</mapper>
