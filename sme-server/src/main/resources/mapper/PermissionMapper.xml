<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.PermissionMapper">

    <!-- 基础结果映射（不含关联字段） -->
    <resultMap id="BasePermissionResultMap" type="com.sme.entity.SysPermission">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="type" property="type" />
        <result column="path" property="path" />
        <result column="icon_id" property="iconId" />
        <result column="sort" property="sort" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 关联图标表的结果映射 -->
    <resultMap id="PermissionResultMap" type="com.sme.entity.SysPermission" extends="BasePermissionResultMap">
        <!-- 关联查询图标地址 -->
        <result column="icon_url" property="iconUrl" />
    </resultMap>

    <!-- 分页查询（联表查询图标地址） -->
    <select id="getPermissions" resultMap="PermissionResultMap">
        SELECT p.*, i.icon_url
        FROM sys_permission p
        LEFT JOIN sys_icon i ON p.icon_id = i.id AND i.del_flag = 0
        WHERE p.del_flag = 0
        <if test="name != null and name != ''">
            AND p.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="code != null and code != ''">
            AND p.code LIKE CONCAT('%', #{code}, '%')
        </if>
        ORDER BY p.sort
    </select>

    <!-- 根据id查询（联表查询图标地址） -->
    <select id="getPermissionById" resultMap="PermissionResultMap">
        SELECT p.*, i.icon_url
        FROM sys_permission p
                 LEFT JOIN sys_icon i ON p.icon_id = i.id AND i.del_flag = 0
        WHERE p.id = #{id} AND p.del_flag = 0
    </select>

    <!-- 新增（移除icon字段，保留icon_id） -->
    <insert id="addPermission">
        INSERT INTO sys_permission (name, code, type, path, icon_id, sort, create_time, del_flag)
        VALUES (#{name}, #{code}, #{type}, #{path}, #{iconId}, #{sort}, CURRENT_TIMESTAMP, 0)
    </insert>

    <!-- 更新（移除icon字段，保留icon_id） -->
    <update id="updatePermission">
        UPDATE sys_permission
        SET name = #{name},
            code = #{code},
            type = #{type},
            path = #{path},
            icon_id = #{iconId},
            sort = #{sort},
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id} AND del_flag = 0
    </update>

    <!-- 删除（逻辑删除） -->
    <update id="deletePermission">
        UPDATE sys_permission
        SET del_flag = 1, update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <!-- 路径唯一性校验（修复参数绑定） -->
    <select id="checkPathUnique" resultType="int">
        SELECT COUNT(1)
        FROM sys_permission
        WHERE path = #{path} AND del_flag = 0
        <if test="id != null">
            AND id != #{id}
        </if>
    </select>

    <!-- 查询所有权限（联表查询图标地址） -->
    <select id="selectAllPermissions" resultMap="PermissionResultMap">
        SELECT p.*, i.icon_url
        FROM sys_permission p
                 LEFT JOIN sys_icon i ON p.icon_id = i.id AND i.del_flag = 0
        WHERE p.del_flag = 0
        ORDER BY p.sort ASC
    </select>

</mapper>