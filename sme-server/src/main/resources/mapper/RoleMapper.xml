<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.RoleMapper">

    <!--    结果映射-->
    <resultMap id="RoleResultMap" type="com.sme.entity.Role">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="role_code" property="roleCode" />
        <result column="description" property="description" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    <insert id="addRole">
        insert into sys_role(role_name, role_code, description, create_time, update_time)
        values(#{roleName}, #{roleCode}, #{description},  NOW(), NOW())
    </insert>
    <update id="updateRole">
        update sys_role
        set role_name = #{roleName}, role_code = #{roleCode}, description = #{description}, update_time = NOW()
        where id = #{id}
    </update>
    <delete id="deleteRole">
        delete from sys_role where id = #{id}
    </delete>

    <!-- 分页查询角色） -->
    <select id="pageQuery" resultMap="RoleResultMap">
        select * from sys_role
        <if test="roleName != null and roleName != ''">
            AND role_name LIKE CONCAT('%', #{roleName}, '%')
        </if>
        <if test="description != null and description != ''">
            AND description LIKE CONCAT('%', #{description}, '%')
        </if>
        <if test="userId != null">
            AND id IN (SELECT role_id FROM user_role WHERE user_id = #{userId})
        </if>
    </select>

    <select id="selectAllRoles" resultType="com.sme.entity.Role">
        SELECT * FROM sys_role WHERE del_flag = 0 ORDER BY id ASC
    </select>
    <select id="checkRoleCodeUnique" resultType="int">
        SELECT COUNT(1)
        FROM sys_role
        WHERE role_code = #{roleCode}
        <if test="id != null">
            AND id != #{id}
        </if>
    </select>
    <select id="getRoleById" resultType="com.sme.entity.Role">
        select * from sys_role where id = #{id}
    </select>

</mapper>