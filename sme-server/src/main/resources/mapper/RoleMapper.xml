<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.RoleMapper">

    <!--    结果映射-->

    <resultMap id="RoleResultMap" type="com.sme.entity.Role">
        <id column="id" property="id" />
        <result column="role_name" property="roleName" />
        <result column="role_code" property="roleCode" />
        <result column="description" property="description" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

        <select id="findRolesByUserId" parameterType="long" resultMap="RoleResultMap">
            select r.* from sys_role r
                                inner join user_role ur on r.id = ur.role_id
            where ur.user_id = #{userId} and r.status = 1
        </select>

        <select id="findById" parameterType="long" resultMap="RoleResultMap">
            select * from sys_role where id = #{id} and status = 1
        </select>

        <select id="findAll" resultMap="RoleResultMap">
            select * from sys_role where status = 1
        </select>

    <insert id="insert" parameterType="com.sme.entity.Role" useGeneratedKeys="true" keyProperty="id">
        insert into sys_role
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="roleName != null">role_name,</if>
            <if test="roleCode != null">role_code,</if>
            <if test="description != null">description,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="roleName != null">#{roleName},</if>
            <if test="roleCode != null">#{roleCode},</if>
            <if test="description != null">#{description},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="update" parameterType="com.sme.entity.Role">
        update sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleCode != null and roleCode != ''">role_code = #{roleCode},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

        <delete id="delete" parameterType="long">
            delete from sys_role where id = #{id}
        </delete>

        <insert id="insertRolePermission">
            insert into role_permission(role_id, permission_id) values(#{roleId}, #{permissionId})
        </insert>

        <delete id="deleteRolePermission" parameterType="long">
            delete from role_permission where role_id = #{roleId}
        </delete>

        <select id="findPermissionIdsByRoleId" parameterType="long" resultType="long">
            select permission_id from role_permission where role_id = #{roleId}
        </select>

</mapper>
