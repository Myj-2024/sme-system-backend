<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sme.mapper.DictMapper">
    <resultMap id="DictResultMap" type="com.sme.entity.Dict">
        <id column="id" property="id"/>
        <result column="dict_code" property="dictCode"/>
        <result column="dict_name" property="dictName"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <!-- 分页查询字典主表 -->
    <select id="selectDictPage" resultMap="DictResultMap">
        SELECT * FROM sys_dict
        WHERE del_flag = 0
        <if test="dictCode != null and dictCode != ''">
            AND dict_code LIKE CONCAT('%', #{dictCode}, '%')
        </if>
        <if test="dictName != null and dictName != ''">
            AND dict_name LIKE CONCAT('%', #{dictName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort ASC, create_time DESC
    </select>

    <!-- 根据ID查询 -->
    <select id="selectDictById" resultMap="DictResultMap">
        SELECT * FROM sys_dict WHERE id = #{id}
    </select>

    <!-- 根据编码查询 -->
    <select id="selectDictByCode" resultMap="DictResultMap">
        SELECT * FROM sys_dict WHERE dict_code = #{dictCode} AND del_flag = 0
    </select>

    <select id="selectDictByName" resultMap="DictResultMap">
        SELECT * FROM sys_dict WHERE dict_name = #{dictName}
    </select>

    <select id="selectDictByNameAndNotId" resultMap="DictResultMap">
        SELECT * FROM sys_dict WHERE dict_name = #{dictName} AND id != #{id}
    </select>

    <!-- 新增字典 -->
    <insert id="insertDict" parameterType="com.sme.entity.Dict">
        INSERT INTO sys_dict (dict_code, dict_name, sort, status, create_time, del_flag)
        VALUES (#{dictCode}, #{dictName}, #{sort}, #{status}, NOW(), 0)
    </insert>

    <!-- 修改字典 -->
    <update id="updateDict" parameterType="com.sme.entity.Dict">
        UPDATE sys_dict
        SET dict_code = #{dictCode},
            dict_name = #{dictName},
            sort = #{sort},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <!-- 逻辑删除 -->
    <update id="deleteDictById">
        UPDATE sys_dict
        SET del_flag = 1, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 修改状态 -->
    <update id="updateDictStatus">
        UPDATE sys_dict
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>
</mapper>