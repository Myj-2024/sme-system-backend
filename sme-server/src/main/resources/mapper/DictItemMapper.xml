<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sme.mapper.DictItemMapper">
    <resultMap id="DictItemResultMap" type="com.sme.entity.DictItem">
        <id column="id" property="id"/>
        <result column="dict_id" property="dictId"/>
        <result column="item_code" property="itemCode"/>
        <result column="item_name" property="itemName"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="del_flag" property="delFlag"/>
    </resultMap>

    <!-- 原有方法保持不变 -->
    <select id="selectDictItemByDictId" resultMap="DictItemResultMap">
        SELECT * FROM sys_dict_item
        WHERE dict_id = #{dictId} AND del_flag = 0
        ORDER BY sort ASC
    </select>

    <insert id="batchInsertDictItem">
        INSERT INTO sys_dict_item (dict_id, item_code, item_name, sort, status, create_time, del_flag)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.dictId}, #{item.itemCode}, #{item.itemName}, #{item.sort}, #{item.status}, NOW(), 0)
        </foreach>
    </insert>

    <update id="deleteDictItemByDictId">
        UPDATE sys_dict_item
        SET del_flag = 1, update_time = NOW()
        WHERE dict_id = #{dictId}
    </update>

    <update id="updateDictItem" parameterType="com.sme.entity.DictItem">
        UPDATE sys_dict_item
        SET item_code = #{itemCode},
            item_name = #{itemName},
            sort = #{sort},
            status = #{status},
            update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <update id="deleteDictItemById">
        UPDATE sys_dict_item
        SET del_flag = 1, update_time = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateDictItemStatus">
        UPDATE sys_dict_item
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id} AND del_flag = 0
    </update>

    <select id="selectDictItemByDictCode" resultType="com.sme.entity.DictItem">
        SELECT di.*
        FROM sys_dict_item di
                 LEFT JOIN sys_dict d ON di.dict_id = d.id
        WHERE d.dict_code = #{dictCode}
          AND d.del_flag = 0
          AND d.status = 1
          AND di.del_flag = 0
          AND di.status = 1
        ORDER BY di.sort ASC
    </select>

    <!-- 核心修改：移除 del_flag = 0，查询全量数据（含逻辑删除） -->
    <select id="selectDictItemByDictIdAndCode" resultMap="DictItemResultMap">
        SELECT * FROM sys_dict_item WHERE dict_id = #{dictId} AND item_code = #{itemCode}
    </select>

    <select id="selectDictItemByDictIdAndName" resultMap="DictItemResultMap">
        SELECT * FROM sys_dict_item WHERE dict_id = #{dictId} AND item_name = #{itemName}
    </select>
</mapper>