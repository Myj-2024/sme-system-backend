<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sme.mapper.DeptMapper">

    <!-- 基础结果映射 -->
    <resultMap id="DeptResultMap" type="com.sme.entity.Dept">
        <id column="id" property="id" />
        <result column="dept_name" property="deptName" />
        <result column="dept_code" property="deptCode" />
        <result column="parent_id" property="parentId" />
        <result column="phone" property="phone" />
        <result column="position" property="position" />
        <result column="leader" property="leader" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>
    <insert id="insertDept">
        insert into sys_dept(dept_name,dept_code,parent_id,phone,leader,position,status,create_time,update_time,del_flag)
        values(#{deptName},#{deptCode},#{parentId},#{phone},#{leader},#{position},#{status},#{createTime},#{updateTime},#{delFlag})
    </insert>

    <!-- 修改：按删除状态查询，只返回未删除的部门 -->
    <select id="findAllByDelFlag" resultMap="DeptResultMap">
        select * from sys_dept where del_flag = #{delFlag}
    </select>

</mapper>